# XYLEX Group session context

You are working with the XYLEX Group agent skills plugin.

- Ecosystem graph: `xylex.md` (use `⤳ skill:` links)
- Skills live under `skills/<name>/SKILL.md` with YAML frontmatter
- Validate with `bun run validate` (add `--coverage skip` offline)
- Scaffold skills with the `create-xylex-skill` skill
